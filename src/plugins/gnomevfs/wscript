def build(bld):
  obj = bld.create_obj('cc', 'shlib')
  obj.target = 'gnomevfs'
  obj.sources = 'gnomevfs.c'
  obj.uselib = 'gnomevfs'

def configure(conf):
  if conf.check_pkg('gnome-vfs-2.0', destvar='gnomevfs'):
    conf.env['XMMS_PLUGINS_ENABLED'].append('gnomevfs')
