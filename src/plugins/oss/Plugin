target="oss"
source=["oss.c"]

def config(env):
	env.checkheader("sys/soundcard.h")
	try:
		env.checklib("ossaudio", "some_symbol_I_dont_know")
	except ConfigError:
		pass #only some platforms need it...

