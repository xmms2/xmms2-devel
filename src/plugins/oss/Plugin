"""
This is a plugin build file for XMMS2
"""

import sys

# first we tell scons what our targets are
target="oss"
# then we tell scons where our sources are
source=["oss.c"]

supported_platforms = ['freebsd', 'linux', 'netbsd', 'openbsd']

def config(env):
	""" 
	This function should return True if this module
	is to be built otherwise False
	"""

	# this should really be done checking
	# for "soundcard.h" and OSS_GETVERSION
	

	if env.platform == 'netbsd' or env.platform == 'openbsd':
		env["LIBS"].append("ossaudio")

        return True
