def build(bld):
    obj = bld.create_obj('cc', 'shlib')
    obj.target = 'xmms_alsa'
    obj.source = 'alsa.c'
    obj.uselib = 'alsa'

def configure(conf):
    if conf.check_pkg('alsa', destvar='alsa'):
        conf.env['XMMS_PLUGINS_ENABLED'].append('alsa')
