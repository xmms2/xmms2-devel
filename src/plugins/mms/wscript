from waftools.plugin import plugin

def plugin_configure(conf):
	return (conf.check_pkg('mms', destvar='mms') or
					conf.check_pkg('libmms', destvar='mms'))

configure, build = plugin('mms', configure=plugin_configure, needs_lib=True)
