from waftools.plugin import plugin

def plugin_configure(conf):
	return (conf.check_pkg('modplug', destvar='modplug') or
					conf.check_pkg('libmodplug', destvar='modplug'))

configure, build = plugin('modplug', configure=plugin_configure, needs_lib=True)
