from waftools.plugin import plugin
import Params

def plugin_configure(conf):
    platform_names = ['netbsd', 'openbsd']

    if not filter(lambda x: Params.g_platform.startswith(x), platform_names):
        return False

    return True

configure, build = plugin('sun', configure=plugin_configure, output_prio=33)
