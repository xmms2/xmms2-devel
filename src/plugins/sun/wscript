from waftools.plugin import plugin
import Options

def plugin_configure(conf):
    if [x for x in ("netbsd", "openbsd") if Options.platform.startswith(x)]:
        return True
    return False

configure, build = plugin("sun", configure=plugin_configure, output_prio=33)
