from xmmsenv.plugin import plugin

def plugin_configure(conf):
  return conf.check_pkg('mad', destvar='mad')

configure, build = plugin('mad', configure=plugin_configure,
                          source=['mad.c', 'xing.c', 'id3v1.c'],
                          needs_lib=True)
