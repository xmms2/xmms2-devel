target = "xmmstypes"

source = ["coll.c", "xlist.c"]

shared = False
static = True
install = False

def config(env):
	env.Append(CPPPATH=["#src/includepriv"])
	if env.platform != 'win32':
		env.Append(CCFLAGS=["-fPIC"])
